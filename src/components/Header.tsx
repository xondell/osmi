'use client';

export default function Header() {
  return (
    <header className="fixed top-6 md:top-8 left-6 md:left-8 z-50 mix-blend-difference">
      <span className="text-white font-sans font-bold text-xl tracking-tighter lowercase select-none">
        osmi
      </span>
    </header>
  );
}
